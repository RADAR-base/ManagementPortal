
dependencies {
    implementation group: 'org.mapstruct', name: 'mapstruct', version: libs.versions.mapstruct.get()
    kapt group: 'org.mapstruct', name: 'mapstruct-processor', version: libs.versions.mapstruct.get()
}
